# But de ce repository

Un flux d'événements de données liées (LDES) est une collection de membres décrits à l'aide du cadre de description des ressources (RDF). La spécification explique comment un client peut reproduire l'historique d'un flux d'événements et comment il peut ensuite rester synchronisé lorsque de nouveaux membres sont publiés.

